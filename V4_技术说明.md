# V4 — 抖音转写流水线 技术说明

> 版本：v4.0  
> 最后更新：2026-05-05  
> 项目目录：`~/douyin_api/versions/v4/`

---

## 一、概述

V4 是一个**全自动抖音视频转写 + WordPress 发布流水线**。

它从抖音账号抓取视频列表，自动下载视频、提取音频、GPU 转写为文字，最后格式化排版并发布到 WordPress 网站。整个过程无需人工干预，7 个模块独立运行，通过 SQLite 数据库协作。

### 核心能力

| 能力 | 说明 |
|------|------|
| 自动采集 | 按配置的抖音账号自动扫描新视频 |
| 视频下载 | Chrome CDP 获取直链 → 即时下载 + 音频提取 |
| GPU 转写 | faster-whisper large-v3 (CUDA)，模型常驻内存 |
| WordPress 发布 | HTML 格式化排版 + 特色图片生成 |
| 去重机制 | 基于 V3 已完成 ID 列表跳过已处理视频 |
| 自动恢复 | 模块崩溃后 5 秒自动重启 |
| 状态追踪 | SQLite 统一管理所有视频状态 |

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────┐
│                V4 管道数据流                         │
├───────────┬──────────┬──────────┬───────────────────┤
│  Scanner  │→ pending →│URL       │→ downloaded →     │
│  (扫描)   │          │Fetcher   │                   │
│           │          │(下载+提取)│                   │
├───────────┴──────────┼──────────┼───────────────────┤
│                      │          │                   │
│                      ▼          ▼                   │
│              ┌──────────────┐                       │
│              │  SQLite 数据库 │←── Transcriber       │
│              │  pipeline.db │    (GPU 转写)         │
│              └──────────────┘                       │
│                      │                              │
│                      ▼                              │
│              ┌──────────────┐                       │
│              │   Publisher   │──→ WordPress         │
│              │  (格式化发布)  │    (localhost:8080)  │
│              └──────────────┘                       │
│                                                    │
│  Monitor ─── 定时检查全部模块健康                      │
│  Cleaner ─── 清理缓存、备份数据库                      │
└─────────────────────────────────────────────────────┘
```

### 模块关系

| 模块 | 输入 | 输出 | 数据流向 |
|------|------|------|----------|
| scanner | 抖音 API | 视频记录 | → DB (pending) |
| url_fetcher | DB (pending) | mp4 + mp3 | → DB (downloaded) |
| transcriber | mp3 音频 | md 文稿 | → DB (done) |
| publisher | md 文稿 | WP 文章 | → DB (published) |
| monitor | 系统状态 | 报告 | 只读不写 |
| cleaner | 缓存文件 | 清理 | 删除文件 + DB 备份 |

> **注意**：downloader 模块已废弃，下载+提取音频整合到 url_fetcher 中。

---

## 三、数据库结构

### 数据库位置

```
~/douyin_api/versions/v4/database/pipeline.db
```

### 数据表

#### `videos` — 视频状态表（核心表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT | 抖音视频 ID（主键） |
| account | TEXT | 作者名称 |
| desc | TEXT | 视频描述/标题 |
| sec_uid | TEXT | 抖音用户 UID |
| aweme_id | TEXT | 抖音作品 ID |
| create_time | TEXT | 视频发布时间 |
| status | TEXT | **状态枚举**（见下方） |
| retry_count | INTEGER | 重试次数 |
| error | TEXT | 错误信息（失败时记录） |
| download_url | TEXT | 视频下载链接 |
| mp4_path | TEXT | 本地 mp4 文件路径 |
| mp3_path | TEXT | 本地 mp3 文件路径 |
| output_path | TEXT | 输出 md 文件路径 |
| wp_post_id | INTEGER | WordPress 文章 ID |
| updated_at | TEXT | 更新时间戳 |

**status 状态流转**：

```
pending → downloaded → transcribing → done → published
                                            ↓
                                         (最终态)
```

- **pending**: 已扫描，等待下载
- **downloaded**: 已下载+提取音频，等待转写
- **transcribing**: GPU 转写中
- **done**: 转写完成，md 文件已输出
- **published**: 已发布到 WordPress
- **url_failed**: URL 获取失败
- **download_failed**: 下载失败
- **pub_failed**: 发布失败

#### `accounts` — 账号配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| name | TEXT | 账号名称 |
| sec_uid | TEXT | 抖音 sec_uid |
| enabled | INTEGER | 是否启用 |
| created_at | TEXT | 创建时间 |

#### `pipeline_log` — 管道日志表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增 ID |
| module | TEXT | 模块名 |
| level | TEXT | 日志级别 (info/warning/error) |
| message | TEXT | 日志内容 |
| video_id | TEXT | 关联视频 ID（可选） |
| created_at | TEXT | 时间戳 |

#### `config` — 键值配置表

| 字段 | 类型 | 说明 |
|------|------|------|
| key | TEXT | 配置键 |
| value | TEXT | 配置值 |

---

## 四、配置说明

配置文件：`~/douyin_api/versions/v4/config/config.toml`

### 基本结构

```toml
[general]
project_root = "/path/to/versions/v4"

[database]
path = "database/pipeline.db"

[accounts]
[[accounts.entries]]
name = "棱镜Talk"
sec_uid = "your_douyin_sec_uid_1"
enabled = true
# ... 更多账号 ...

[chrome]
cdp_url = "http://127.0.0.1:9222"

[cookies]
file = "/path/to/douyin_cookies.json"

[wordpress]
url = "http://your-wordpress-site:8080"
username = "admin"
password = "your_admin_password"
generate_featured_image = true

[cleaner]
max_cache_gb = 10
keep_days = 7
```

### 如何添加新抖音账号？

1. 打开 `config/config.toml`
2. 在 `[accounts]` 段末尾添加：

```toml
[[accounts.entries]]
name = "你选的账号名称"
sec_uid = "抖音用户的 sec_uid"
enabled = true
```

3. **获取 sec_uid 的方法**：
   - 在浏览器打开抖音用户主页
   - URL 类似 `https://www.douyin.com/user/MS4wLjABAAAA...`
   - `MS4wLjABAAAA...` 那串就是 sec_uid
4. 重启 scanner：`kill $(pgrep -f "modules/scanner")`

---

## 五、使用指南

### 环境要求

- **Python**: 3.10+
- **GPU**: NVIDIA + CUDA 12（显存 ≥ 8GB，推荐 16GB）
- **软件**: ffmpeg, ImageMagick, Chrome/Chromium
- **Python 包**:
  - `faster-whisper`（GPU 转写）
  - `websocket-client`（Chrome CDP 通信）
  - `Pillow`（特色图片生成）
  - `requests`, `urllib3`

### 启动

**一键启动所有模块**：

```bash
cd ~/douyin_api/versions/v4
bash scripts/start_v4.sh
```

**或逐个启动**（在 tmux 窗口中分别运行）：

```bash
# 先启动 tmux session
tmux new-session -d -s douyin-v4

# 每个模块开一个窗口（间隔3秒防DB竞态）
cd ~/douyin_api/versions/v4

# 窗口1: scanner
python3 modules/scanner.py

# 窗口2: url_fetcher
python3 modules/url_fetcher.py

# 窗口3: transcriber（自动启动worker）
python3 modules/transcriber.py

# 窗口4: publisher
python3 modules/publisher.py

# 窗口5: monitor
python3 modules/monitor.py

# 窗口6: cleaner
python3 modules/cleaner.py
```

### 查看状态

**完整报告**：

```bash
cd ~/douyin_api/versions/v4
python3 modules/monitor.py --report
```

输出示例：
```
📊 抖音转写流水线 — 状态报告
💻 CPU: 21.9%  |  内存: 17/30GB
🎮 GPU: 94%  |  显存: 4.5/16GB
📈 已发布: 176篇  |  待处理: 327个
📝 总字数: 19.1万字
```

**快速状态**：

```bash
bash scripts/status.sh
```

### 查看日志

```bash
# 查看所有模块最新日志
tail -f ~/douyin_api/versions/v4/logs/*.log

# 查看特定模块
tail -f ~/douyin_api/versions/v4/logs/scanner.log
tail -f ~/douyin_api/versions/v4/logs/transcriber.log
tail -f ~/douyin_api/versions/v4/logs/publisher.log
```

### 停止所有模块

```bash
# 精确停止（推荐 - 只停 v4 模块）
ps aux | grep 'modules/.*\.py' | grep -v grep | awk '{print $2}' | xargs kill

# 或用 tmux 关闭
tmux kill-session -t douyin-v4
```

### 输出目录

```
~/douyin_api/versions/v4/output/
├── 小Lin说/
│   └── 2026-05-05_标题_视频ID.md
├── 棱镜Talk/
│   └── ...
├── 金灿荣教授/
│   └── ...
└── 王德峰/
    └── ...
```

每篇文稿格式：

```markdown
---
source: douyin
account: 棱镜Talk
video_id: 7177654120130743607
date: 2026-05-05 19:56
word_count: 3913
---

# 文章标题

[0.0] 第1句转写文本
[4.1] 第2句转写文本
[4.8] 第3句转写文本
...
```

### WordPress 访问

- 地址：`http://your-wordpress-site:8080`
- 管理员：`admin` / `your_password`
- 已发布文章可在后台"文章"中查看

---

## 六、管道状态流转详解

```
Scanner                     URL Fetcher                  Transcriber                  Publisher
   │                            │                            │                            │
   ▼                            ▼                            ▼                            ▼
┌──────┐   扫描新视频    ┌──────────┐   下载+提取    ┌────────────┐   GPU转写    ┌──────────┐  → WP
│ 抖音 ├──────────────→ │ pending  ├─────────────→ │ downloaded ├───────────→ │   done   ├──→ published
│ API  │                │          │               │            │             │          │
└──────┘                └──────────┘               └────────────┘             └──────────┘
                              │                          │                         │
                              │ 失败3次                   │ 失败                     │ 失败
                              ▼                          ▼                         ▼
                         url_failed                transcribe_failed          pub_failed
```

### 失败处理

- **URL 获取失败**: 重试3次 → 标记 `url_failed`
- **下载失败**: 重试3次 → 标记 `download_failed`
- **转写失败**: 标记 `transcribe_failed`
- **发布失败**: 标记 `pub_failed`

所有模块都有 while 循环自动重启，崩溃后 5 秒内自动恢复。

---

## 七、与 V3 的关系

| 对比项 | V3 | V4 |
|--------|----|----|
| 架构 | 单体脚本 | 7 模块微服务 |
| 去重 | 无 | 基于 V3 ID 列表 |
| 下载 | curl（易被拒） | Chrome CDP + urllib |
| 转写 | 每次加载模型 | 常驻 Worker |
| 发布 | 无 WordPress 集成 | 自动格式化发布 |
| 输出 | 有时间戳分段 | 有时间戳分段 ✅ |
| 监控 | 无 | 模块健康 + 管道数据流 |

**V4 去重机制**：`data/v3_done_ids.json` 包含 V3 已处理的 9255 个视频 ID。
Scanner 在插入新视频前会检查该列表，跳过已处理视频。

---

## 八、关键文件路径

| 文件/目录 | 用途 |
|-----------|------|
| `~/douyin_api/versions/v4/` | V4 根目录 |
| `config/config.toml` | 所有配置 |
| `modules/scanner.py` | 扫描模块 |
| `modules/url_fetcher.py` | 下载+提取音频模块 |
| `modules/transcriber.py` | 转写管理器 |
| `modules/transcriber_worker.py` | 转写工作进程（GPU 常驻） |
| `modules/publisher.py` | WP 发布模块 |
| `modules/monitor.py` | 监控模块 |
| `modules/cleaner.py` | 清理模块 |
| `lib/db.py` | 数据库操作层 |
| `lib/config.py` | 配置读取层 |
| `lib/wordpress.py` | WordPress API 层 |
| `database/pipeline.db` | SQLite 数据库 |
| `logs/*.log` | 各模块日志 |
| `output/*` | 转写文稿输出 |
| `tmp_cache/*` | mp4/mp3 临时缓存 |
| `data/v3_done_ids.json` | V3 已完成 ID 去重列表 |
| `scripts/start_v4.sh` | 一键启动脚本 |
| `scripts/status.sh` | 状态查看脚本 |

---

## 九、常见问题

### Q: 模块退出了会自动重启吗？
是的。每个模块由 `while true; do python3 modules/X.py; sleep 5; done` 包装，崩溃后 5 秒自动重启。

### Q: 如何防止重复下载？
V4 启动时检查 `data/v3_done_ids.json`（V3 已完成 ID）。同时 DB 内每个视频有唯一 ID，不会重复插入。

### Q: 日志太大怎么办？
每个模块日志限制 10MB，自动轮转保留 3 份旧日志。

### Q: 数据库会被锁死吗？
SQLite 使用 WAL 模式 + `busy_timeout=60000` + 写操作自动重试（5 次指数退避），多模块并发安全。

### Q: 如何增加转写速度？
- 升级 GPU（RTX 4090 可提速 3-5 倍）
- 或换小模型（config.toml: `model = "medium"`）
- 或降低精度（`compute_type = "float16"`）

---

## 十、版本管理

```
~/douyin_api/
├── versions/
│   ├── v3/              # V3 快照（源码+输出）
│   └── v4/              # V4 当前版本（当前活跃）
├── V4_技术说明.md        # 本文档
├── VERSIONS.md          # 版本历史
└── ARCHITECTURE.md      # 架构设计文档
```

Git 仓库：

```bash
cd ~/douyin_api
git add .
git commit -m "v4: xxx功能"
```

---

*— 文档结束 —*
