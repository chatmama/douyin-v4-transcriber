# V4 抖音转写管道 — Obsidian 可视化

> 项目根目录：`~/douyin_api/`  
> 当前版本：v4.0

---

## 1. 数据流总览

```mermaid
flowchart TD
    A[抖音 API] --> B[Scanner<br/>扫描模块]
    B --> C[(SQLite<br/>pipeline.db)]
    C --> D[URL Fetcher<br/>下载+提取音频]
    D --> C
    C --> E[Transcriber<br/>GPU 转写]
    E --> F[(MD 文稿)]
    E --> C
    C --> G[Publisher<br/>WordPress 发布]
    G --> H[WordPress<br/>localhost:8080]
    
    I[Monitor] -.->|定时检查| B
    I -.->|定时检查| D
    I -.->|定时检查| E
    I -.->|定时检查| G
    
    J[Cleaner] -.-> K[清理缓存<br/>备份数据库]
```

---

## 2. 模块角色

```mermaid
flowchart LR
    subgraph 采集层
        M1[Scanner<br/>扫描抖音 API<br/>获取视频列表<br/>写入 DB: pending]
    end
    subgraph 处理层
        M2[URL Fetcher<br/>CDP 取直链<br/>下载 mp4<br/>提取 mp3<br/>写入 DB: downloaded]
    end
    subgraph 转写层
        M3[Transcriber<br/>faster-whisper<br/>GPU large-v3<br/>输出 md 文稿<br/>写入 DB: done]
    end
    subgraph 发布层
        M4[Publisher<br/>HTML 格式化<br/>特色图片<br/>WordPress API<br/>写入 DB: published]
    end
    subgraph 运维层
        M5[Monitor<br/>GPU 利用率<br/>磁盘空间<br/>管道速度]
        M6[Cleaner<br/>删除缓存 mp4<br/>备份 DB<br/>日志轮转]
    end
    
    M1 --> M2 --> M3 --> M4
    M5 -.-> M1 & M2 & M3 & M4
    M6 -.->|清理| M2 & M3
```

---

## 3. 数据库状态流转

```mermaid
stateDiagram-v2
    [*] --> pending : scanner 扫描到新视频
    pending --> downloaded : url_fetcher 下载成功
    pending --> url_failed : 重试3次失败
    downloaded --> transcribing : transcriber 开始转写
    transcribing --> done : GPU 转写完成
    done --> published : publisher 发布到 WP
    done --> pub_failed : WP 发布失败
    url_failed --> [*] : 标记失败
    download_failed --> [*] : 标记失败
    transcribing --> transcribe_failed : 转写出错
    pub_failed --> done : 可手动重试发布
    published --> [*] : ✅ 完成
```

---

## 4. 技术栈

```mermaid
quadrantChart
    title 技术栈分布
    x-axis "基础设施" --> "业务逻辑"
    y-axis "输入" --> "输出"
    quadrant-1 "GPU 计算"
    quadrant-2 "网络交互"
    quadrant-3 "数据存储"
    quadrant-4 "展示层"
    "SQLite": [0.2, 0.3]
    "faster-whisper": [0.8, 0.7]
    "Chrome CDP": [0.7, 0.4]
    "WordPress REST": [0.9, 0.2]
    "ffmpeg": [0.5, 0.6]
    "ImageMagick": [0.6, 0.2]
    "Python 3.13": [0.4, 0.5]
```

---

## 5. 配置文件结构

```
config/config.toml
├── [general]          ─── 项目路径、日志目录
├── [database]         ─── SQLite 路径
├── [accounts]         ─── 账号列表（可追加）
│   ├── 棱镜Talk
│   ├── 小Lin说
│   ├── 金灿荣教授
│   └── 王德峰
├── [scanner]          ─── 扫描间隔
├── [transcriber]      ─── 模型、精度、并发
├── [chrome]           ─── CDP 地址
├── [cookies]          ─── Cookie 文件路径
├── [wordpress]        ─── WP 地址/账号
├── [cleaner]          ─── 缓存上限
└── [monitor]          ─── 检查间隔
```

---

## 6. 文件组织结构

```
versions/v4/
├── modules/            ← 7个执行模块
│   ├── scanner.py
│   ├── url_fetcher.py
│   ├── transcriber.py       (管理器)
│   ├── transcriber_worker.py (GPU 工作进程)
│   ├── publisher.py
│   ├── monitor.py
│   └── cleaner.py
├── lib/                ← 公共库
│   ├── db.py           ← 数据库层
│   ├── config.py       ← 配置层
│   └── wordpress.py    ← WP API 层
├── config/
│   └── config.toml     ← 所有配置
├── database/
│   └── pipeline.db     ← SQLite (WAL模式)
├── logs/               ← 模块日志 (10MB轮转)
├── output/             ← 转写文稿
├── tmp_cache/          ← mp4/mp3缓存
├── data/
│   └── v3_done_ids.json ← V3去重列表
└── scripts/
    ├── start_v4.sh     ← 一键启动
    └── status.sh       ← 状态查看
```

---

## 7. 关键路径

> [!info] 数据库
> `~/douyin_api/versions/v4/database/pipeline.db`
> 所有模块通过它协作（状态机模式）

> [!info] 日志
> `~/douyin_api/versions/v4/logs/*.log`
> 每个模块独立日志，自动轮转

> [!info] 去重
> `~/douyin_api/versions/v4/data/v3_done_ids.json`
> 9255个V3已完成ID，scanner据此跳过

> [!info] 缓存
> `~/douyin_api/versions/v4/tmp_cache/`
> cleaner 自动清理超过 7 天的缓存

---

## 8. 发布文章展示

```mermaid
flowchart LR
    subgraph WordPress
        WP1[文章列表] --> WP2[标题 + 特色图片]
        WP2 --> WP3[正文: 全文HTML]
        WP3 --> WP4[时间戳分段<br/><p>标签排版]
    end
    subgraph 生成过程
        G1[MD文稿] --> G2[提取标题]
        G1 --> G3[保留时间戳分段]
        G2 --> G4[ImageMagick生成封面]
        G3 --> G5[HTML格式化]
        G4 --> G6[WP REST上传]
        G5 --> G6
    end
    G6 --> WP1
```

---

## 9. 当前管道状态

> [!note] 最近状态（2026-05-05 20:04）
> - 已发布：164 篇
> - 待处理：337 个
> - GPU 利用率：94%
> - 总转写字数：11.8 万字
> - Chrome CDP：在线

---

## 10. 相关链接

- [[V4_技术说明|完整技术文档]]
- [[VERSIONS|版本历史]]
- [[ARCHITECTURE|架构设计]]

---

*— 用 [Obsidian](https://obsidian.md/) 打开本文件可渲染所有 Mermaid 图表 —*
